# Kilo Code 业务档案

## 业务目标

Kilo Code 是面向研发人员的 VS Code 智能编码代理，核心目标是在编辑器内通过自然语言完成代码生成、解释、修复、重构、终端命令执行、浏览器自动化和内联补全等研发任务。

## 核心用户

- 使用 VS Code 的开发者：通过侧边栏聊天、命令、上下文引用和工具调用完成日常编码任务。
- 插件维护者：维护 VS Code 扩展、Webview UI、共享包、JetBrains 插件和独立 Agent Runtime。
- 企业或团队管理员：关注模型访问、Token/成本统计、AI 代码统计、配置分发和使用合规。

## 主要流程

1. 用户在 VS Code 中输入任务，选择模型、模式和上下文。
2. 插件收集必要的工作区环境、文件结构、诊断信息和用户上下文。
3. 插件调用配置的模型提供商，并在流式响应中展示文本、推理、工具调用和执行结果。
4. 插件按工具权限、审批策略和模式约束执行文件、终端、浏览器、MCP 等操作。
5. 本地状态、任务历史、配置、代码索引和统计数据按插件规则持久化。

## 关键业务规则

- 插件应优先在用户当前编辑器环境中完成任务，尽量减少跳出 VS Code 的操作。
- 模型请求前的环境上下文构建必须可靠；如果工作区扫描能力不可用，应给出可诊断错误或使用兼容路径，避免界面长期停留在请求中。
- Kilo Code 是 Roo Code 的 fork，修改共享核心代码时应尽量小范围变更，并用 `kilocode_change` 标记降低后续 upstream merge 冲突。
- 涉及发布的功能修复通常需要补充面向用户的 changeset；纯文档或内部工具改动可不需要。

## 当前已知约束

- 项目是 pnpm monorepo，VS Code 扩展主体在 `src/`，Webview UI 在 `webview-ui/`。
- 插件支持多模型、多提供商和多运行环境；修复 provider 以外的问题时应避免引入特定模型假设。
- 本地研发环境是 macOS/arm64，但实际插件用户可能运行在 Windows、Linux 或不同 VS Code 版本中。

## 最近决策

- 2026-06-05：针对新版 VS Code 中 Kilo Code 卡在“API请求...”且 Token 为 0 的问题，优先修复插件侧 ripgrep 二进制路径兼容性。原因是新版 VS Code 已将内置 ripgrep 放在 `@vscode/ripgrep-universal` 目录，而当前 Kilo 5.16.1 只探测旧路径，导致请求前环境扫描阶段可能失败或停滞。
- 2026-06-05：AI 代码统计的 commit 上报巡检发现服务端未收到时，应先在后台自动重新分析并补传，只有自动补传多次失败后才展示给用户手动处理。原因是巡检能发现可恢复的本地候选数据时，手动补传应作为兜底而不是第一反应，避免用户连续看到“上传失败/需补传”卡片。
- 2026-06-10：AI 代码统计继续保留普通 commit 上报，插件侧新增 commit 生命周期事实采集。amend 场景上报旧 commit 被新 commit 替换，reset 场景上报旧 commit 被废弃；复杂 rewrite 第一版只上报弱诊断，不在插件侧做入库归因或历史迁移判断。
- 2026-07-26：Git 状态监听按串行重查处理短时间内连续到达的文件系统通知；当前一次查询尚未结束时到达的新通知不会被直接丢弃，并由五秒一次的轻量状态核验兜底文件系统通知丢失。关联工作树的 HEAD 继续按各自工作树监听，分支引用则从仓库公共 Git 目录监听。一次通知若跨过多个顺序 commit，插件按持久化游标逐个回放并分别读取各自第一父提交差异，不能把多个 commit 的变更合并归到最后一个 SHA。
- 2026-07-26：云请求收到 HTTP 429 时始终保留原请求。服务端未返回 `Retry-After` 时默认暂停一分钟后继续由本地重试定时器处理，避免限流响应被误判为成功并从持久化队列删除。
- 2026-07-26：AI 代码统计本地状态涉及事件正文、待上传标记、commit 报告和候选块等多个文件时，先写可恢复事务，再提交各状态文件；启动时优先回放未完成事务。重复回放的事件按稳定事件 ID 去重，以“宁可幂等重传、不可静默丢事实”为故障原则。三十天本地候选保留期仍可清理过期归因候选，但已经进入上传队列且尚未收到服务端确认的普通事件和 commit 生命周期报告不受该期限影响。
- 2026-07-26：指定单个报告的手动重试运行期间如果又产生新的上传请求，后到调用等待当前上传及补充轮次真正结束；后到的定向重试保留其报告 ID 并逐个执行，后到的自动维护请求才触发一次全量 outbox 扫描。这样既不会把已被自动隔离、只能人工定向重试的报告漏掉，也避免调用方在上传仍进行时提前查询服务端、误判为“未收到”并触发重复重分析。
- 2026-07-26：插件启动和每五分钟维护任务先重传本地普通事件、commit 报告及生命周期报告，再查询服务端接收/归因状态。原因是仅做状态查询无法推动“退出前已落盘、但还没发出”的报告，恢复链路必须主动消费持久化队列。
- 2026-07-26：服务端 commit 状态巡检并发触发时复用并等待同一轮巡检结果，不在已有巡检尚未完成时直接返回旧的本地失败列表，避免手动重试、启动巡检和定时巡检之间出现误导性瞬时状态。
- 2026-07-26：commit 报告尚未上传成功时，只暂缓与该 commit 直接相关的生命周期报告；其他仓库或其他 commit 的生命周期报告继续上传。这样既保留“事实先于失效/替换”的因果顺序，也避免一个永久失败的大报告把全局生命周期队列全部堵住。
- 2026-07-26：普通 generated/accepted 事件每次请求最多上传 1000 条，单轮维护最多连续处理 10 批；每批收到服务端确认后才从本地待上传集合移除。该边界避免长期离线后的积压一次性形成超大 JSON 请求，同时允许后续五分钟维护轮次持续清空积压。
- 2026-07-26：同一个仓库的同一个物理 commit 一旦已经生成并持久化报告，文件监听重复通知或“报告已落盘、游标尚未来得及推进”后的启动回放不再生成第二个随机报告；手动/自动重分析使用显式重分析路径，不受该重复抑制影响。
- 2026-07-26：Token 使用量采用“本地持久化后重试、服务端明确确认后才清除待上传状态”的交付规则。插件启动时立即恢复未确认记录，并每五分钟继续尝试；并发触发方等待同一轮上传及补充轮次完成。超过本地展示保留期但仍未确认的记录不得删除，HTTP 成功但缺少 `accepted=true` 也不能当作已交付。
- 2026-07-26：AI 代码统计本地状态损坏时不得递归删除整个统计目录。状态索引缺失或损坏时，从仍可读取的事件 outbox 恢复待上传标记并保留损坏文件；遇到不兼容的存储版本时，将原目录整体归档后冷启动，保证旧证据仍可人工恢复。事务重放对已写入的稳定事件 ID 做物理去重，避免反复崩溃持续放大队列文件。
- 2026-07-26：本地 JSON 状态若在“完整新文件已写完、正式文件尚未替换”时崩溃，恢复时始终优先保留可完整解析的正式文件，防止旧版或绕过锁的客户端在遗留写意图后已更新的状态被回退。只有正式文件缺失或损坏时，才使用写意图精确指向的完整新文件或上一份已提交备份；未标记的残留新文件不得再根据修改时间覆盖有效正式状态。显式失败的写入标记为已中止，且仅在正式文件无效时回到已提交备份，不让清理失败的新文件“死灰复燃”。
- 2026-07-26：本地 JSON 写入在支持目录 fsync 的文件系统上，按“新文件 fsync、预先写入并 fsync 精确绑定本代新文件与备份路径的写意图、fsync 目录、旧正式文件重命名到该精确备份路径并再次 fsync 目录、新正式文件重命名并 fsync 文件与目录”的顺序提交，只有越过该耐久化边界后才删除备份。这样即使在时钟回拨或修改时间粒度较粗时，正式文件缺失后也不会在多个保留备份之间猜测。Windows 或挂载目录无法由 Node 执行目录 fsync 时，只承诺原子替换与最大化可恢复，不宣称完整断电耐久性；此时持续保留本代备份、上一代备份和写意图。文件 fsync 使用可写句柄，避免 Windows 因只读句柄拒绝 FlushFileBuffers 而导致所有写入回滚。
- 2026-07-26：已落盘的普通事件和 commit 报告优先使用记录时保存的企业身份继续重传，不因用户随后清空当前设置而永久卡住；生命周期事实和 commit 接收状态查询本身不依赖当前邮箱。服务端 2xx 响应若缺少明确接收结构或返回畸形状态项，客户端按失败保留本地记录并输出诊断，不把代理兜底页或错误 JSON 当作成功。服务端明确回执且本地 outbox 已持久化确认后，成功诊断日志属于辅助信息；其写入失败不得把已交付事实倒置成上传失败或触发无意义重传。
- 2026-08-06：企业采集采用“内网可达 + 插件配置身份可信”的生产口径。代码统计和 Token 入口继续允许匿名写入，不新增设备凭据或请求签名；插件一旦配置人员、组织或项目身份，服务端即按该配置归属。业务接受可访问采集地址的内网调用方伪造上报身份或统计数据的风险；客户端仍必须保留字段校验、严格回执、幂等和退避恢复，但这些可靠性机制不代表身份认证。
- 2026-07-26：仓库远端地址、由此派生的项目身份和当前分支不做进程级永久缓存。仓库首次没有远端、运行期间新增或切换远端、切换分支后，后续新事件按当时可验证的 Git 元数据重新识别项目和分支；已经持久化的待重传事实仍保持生成时身份，避免重试期间静默改归属。
- 2026-07-26：VS Code 多窗口共享同一全局存储时，AI 代码与 Token 状态通过跨进程文件锁串行修改，并用持久化 revision 让其他窗口的缓存及时失效；单个窗口内部的“正在修改”状态不得让另一个并发读取绕过锁。普通事件正文采用 4MiB 分段 NDJSON outbox，按流式读取、稳定事件 ID 去重和 crash-safe 压缩维护，避免长期积压时一次性读写无界大文件。
- 2026-07-26：客户端只有在服务端回执的路由类型、报告 ID 以及“新增数 + 重复数”与本次请求严格一致时，才清除本地待上传事实。单条损坏、字段超长、邮箱格式错误、数据库时间越界或单行超出请求预算的旧记录保持待处理但从当前批次跳过，后续有效记录仍可继续交付，避免 poison record 阻断全队列。
- 2026-07-26：普通事件、commit 报告和生命周期报告均设置单批数量、单轮时间、请求重试、文本体积与按实际 payload 大小计算的超时边界。commit 报告若超过后台默认 256MiB 原文或 128MiB 传输体上限，或其冻结证据含数据库时间范围外的值，不再发起注定失败的请求；报告及诊断继续保留，后台自动轮次跳过，只有用户定向重试才重新尝试。已落盘的无效生命周期事实同样持久标记为 blocked 并退出自动重试。生命周期单侧超过 32768 个 commit 时禁止拆成会产生半更新的独立强事件，并阻止仓库观察游标前移；超大生命周期的原子分块协议仍需另行确定。
- 2026-07-26：Token 统计维度不包含 Git 分支。分支可继续作为本地旧状态兼容字段保留，但新上传 payload 不再发送分支，避免同一用户、项目、工具、模型的 Token 因分支切换被重复拆分。
- 2026-08-08：普通 AI 代码事件因人员配置缺失、邮箱无效、协议字段越界或单条请求体超限而暂时无法上传时，原始事实继续留在本地 outbox，并持久记录可恢复性、分类和具体原因；本轮上传必须显示失败或阻塞，不能以 `uploaded=0` 冒充成功。缺配置及可调整请求预算的记录在条件修正后重新校验并上传；确定违反协议的记录停止网络重试，但保留诊断和原始事实。事件已经持久化有效邮箱时，后续不同的当前配置不得改写其归属。
- 2026-07-28：云请求重试队列必须把账号所有权与请求一起原子持久化；切换账号或退出登录时，只有旧队列清空和新所有权都落盘成功后才能恢复发送。旧版无账号所有权的队列按安全优先隔离并清空，不得使用任意当前账号代发；重试结果也只有在队列增删和重试次数持久化成功后才对外发布。
- 2026-07-31：聊天中的“已处理”过程块在模型仍输出中间推理或工具处理过程时默认展开，便于用户观察进展；进入最终结论输出后自动折叠。用户仍可手动展开或收起，自动切换使用平滑过渡，避免过程内容突然跳变。

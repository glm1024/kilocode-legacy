# Kilo Code 业务档案

## 业务目标

Kilo Code 是面向研发人员的 VS Code 智能编码代理，核心目标是在编辑器内通过自然语言完成代码生成、解释、修复、重构、终端命令执行、浏览器自动化和内联补全等研发任务。

## 核心用户

- 使用 VS Code 的开发者：通过侧边栏聊天、命令、上下文引用和工具调用完成日常编码任务。
- 插件维护者：维护 VS Code 扩展、Webview UI、共享包、JetBrains 插件和独立 Agent Runtime。
- 企业或团队管理员：关注模型访问、Token/成本统计、AI 代码统计、配置分发和使用合规。

## 主要流程

1. 用户在 VS Code 中输入任务，选择模型、模式和上下文。
2. 插件收集必要的工作区环境、文件结构、诊断信息和用户上下文。
3. 插件调用配置的模型提供商，并在流式响应中展示文本、推理、工具调用和执行结果。
4. 插件按工具权限、审批策略和模式约束执行文件、终端、浏览器、MCP 等操作。
5. 本地状态、任务历史、配置、代码索引和统计数据按插件规则持久化。

## 关键业务规则

- 插件应优先在用户当前编辑器环境中完成任务，尽量减少跳出 VS Code 的操作。
- 模型请求前的环境上下文构建必须可靠；如果工作区扫描能力不可用，应给出可诊断错误或使用兼容路径，避免界面长期停留在请求中。
- Kilo Code 是 Roo Code 的 fork，修改共享核心代码时应尽量小范围变更，并用 `kilocode_change` 标记降低后续 upstream merge 冲突。
- 涉及发布的功能修复通常需要补充面向用户的 changeset；纯文档或内部工具改动可不需要。

## 当前已知约束

- 项目是 pnpm monorepo，VS Code 扩展主体在 `src/`，Webview UI 在 `webview-ui/`。
- 插件支持多模型、多提供商和多运行环境；修复 provider 以外的问题时应避免引入特定模型假设。
- 本地研发环境是 macOS/arm64，但实际插件用户可能运行在 Windows、Linux 或不同 VS Code 版本中。

## 最近决策

- 2026-06-05：针对新版 VS Code 中 Kilo Code 卡在“API请求...”且 Token 为 0 的问题，优先修复插件侧 ripgrep 二进制路径兼容性。原因是新版 VS Code 已将内置 ripgrep 放在 `@vscode/ripgrep-universal` 目录，而当前 Kilo 5.16.1 只探测旧路径，导致请求前环境扫描阶段可能失败或停滞。
- 2026-06-05：AI 代码统计的 commit 上报巡检发现服务端未收到时，应先在后台自动重新分析并补传，只有自动补传多次失败后才展示给用户手动处理。原因是巡检能发现可恢复的本地候选数据时，手动补传应作为兜底而不是第一反应，避免用户连续看到“上传失败/需补传”卡片。
- 2026-06-10：AI 代码统计继续保留普通 commit 上报，插件侧新增 commit 生命周期事实采集。amend 场景上报旧 commit 被新 commit 替换，reset 场景上报旧 commit 被废弃；复杂 rewrite 第一版只上报弱诊断，不在插件侧做入库归因或历史迁移判断。

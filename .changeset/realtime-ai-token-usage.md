---
"kilo-code": patch
---

Sync AI token usage shortly after model requests, resume persisted uploads after restart, and retain unacknowledged usage until the server explicitly accepts the exact uploaded JSON by SHA-256.

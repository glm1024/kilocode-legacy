---
"kilo-code": patch
---

Keep queued cloud requests available for retry when a rate-limited response omits or returns an invalid retry delay. Persist the cloud retry queue as a versioned, account-owned envelope; serialize storage writes, publish retry outcomes only after durable state changes, quarantine ownerless legacy queues, and keep account switches fail-closed until their clear is durable. Git commit monitoring now retains in-flight notifications, verifies state periodically, watches shared refs correctly from linked worktrees, and replays every commit in a coalesced fast-forward range instead of attributing the whole range to its final SHA. AI code statistics now journals cross-file outbox state, coordinates shared storage across VS Code windows, retains unacknowledged records, recovers the newest complete crash artifact without letting stale artifacts overwrite current state, resumes queued work using persisted identity, validates acknowledgement route/report/count and the SHA-256 of the exact uploaded JSON, preserves concurrent targeted retries, isolates invalid persisted rows before upload, applies size-aware upload timeouts and server payload bounds, suppresses duplicate reports for an already persisted physical commit, and prevents one failed commit report from blocking unrelated lifecycle reports.
